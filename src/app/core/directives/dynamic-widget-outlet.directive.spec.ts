import { describe, it } from "vitest";

describe("DynamicWidgetOutletDirective", () => {
  it.todo("DYN-01: creates a ComponentRef in ViewContainerRef for each config");

  it.todo("DYN-02: renders all components from a config array");

  it.todo("DYN-04: passes static input values via setInput()");

  it.todo(
    "DYN-05: subscribes to observable input and calls setInput() on each emission"
  );

  it.todo(
    "DYN-06: runs effect() for signal input and calls setInput() reactively"
  );

  it.todo("DYN-07: forwards output events to provided handler function");

  it.todo("DYN-08: calls vcr.clear() when directive is destroyed");

  it.todo("DYN-09: observable subscription is cleaned up on directive destroy");
});
