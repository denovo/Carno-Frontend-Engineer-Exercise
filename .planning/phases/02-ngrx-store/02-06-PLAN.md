---
phase: 02-ngrx-store
plan: "06"
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified:
  - src/app/app.config.ts
  - src/app/core/store/index.ts
autonomous: true
requirements: [NGR-01, NGR-02, NGR-03, NGR-04, NGR-05, NGR-06, NGR-07, NGR-08, NGR-09, NGR-10, NGR-11, NGR-12, NGR-13, NGR-14]

must_haves:
  truths:
    - "provideStore({ [tasksFeatureKey]: tasksReducer }) is registered in app.config.ts"
    - "provideEffects([TaskEffects]) is registered in app.config.ts"
    - "provideStoreDevtools is registered with logOnly: !isDevMode()"
    - "The barrel index re-exports all store public symbols so Phase 3 can import from @app/core/store"
    - "ng build completes without errors (store wiring is compile-time verifiable)"
    - "npm test remains green after wiring (no runtime DI errors introduced)"
  artifacts:
    - path: src/app/app.config.ts
      provides: Store, Effects, DevTools providers registered in ApplicationConfig
      contains: "provideStore"
    - path: src/app/core/store/index.ts
      provides: Barrel re-export of all public store symbols
  key_links:
    - from: src/app/app.config.ts
      to: src/app/core/store/reducers/task.reducer.ts
      via: "import { tasksReducer, tasksFeatureKey }"
      pattern: "provideStore"
    - from: src/app/app.config.ts
      to: src/app/core/store/effects/task.effects.ts
      via: "import { TaskEffects }"
      pattern: "provideEffects"
    - from: src/app/core/store/index.ts
      to: "actions/task.actions, reducers/task.reducer, selectors/task.selectors, effects/task.effects"
      via: "export * from"
---

<objective>
Wire the completed store into the Angular application and publish a clean barrel index. This is the integration capstone: app.config.ts registers provideStore, provideEffects, and provideStoreDevtools; store/index.ts re-exports everything so Phase 3 components import from a single path.

Purpose: Without this plan the store exists as tested files that are never connected to the running app. After this plan, `ng build` proves the entire store compiles end-to-end and `npm test` confirms no DI regressions were introduced.
Output: Updated app.config.ts with store providers, updated store/index.ts barrel, verified build.
</objective>

<execution_context>
@/Users/peterichardson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterichardson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ngrx-store/02-RESEARCH.md
@.planning/phases/02-ngrx-store/02-04-SUMMARY.md
@.planning/phases/02-ngrx-store/02-05-SUMMARY.md

<interfaces>
<!-- Current app.config.ts state (Phase 1 output) -->

From src/app/app.config.ts (current — Phase 1):
```typescript
import { ApplicationConfig, provideBrowserGlobalErrorListeners } from "@angular/core";
import { provideRouter } from "@angular/router";
import { routes } from "./app.routes";

export const appConfig: ApplicationConfig = {
  providers: [
    provideBrowserGlobalErrorListeners(),
    provideRouter(routes),
  ],
};
```

<!-- Phase 2 store symbols to wire in -->

From src/app/core/store/reducers/task.reducer.ts:
```typescript
export declare const tasksFeatureKey: string; // "tasks"
export declare const tasksReducer: ActionReducer<TaskState>;
```

From src/app/core/store/effects/task.effects.ts:
```typescript
export declare class TaskEffects { ... }
```

From @ngrx/store:
```typescript
export declare function provideStore(reducers?: ActionReducerMap<any>, config?: StoreConfig): EnvironmentProviders;
export declare function provideStoreDevtools(options?: StoreDevtoolsOptions): EnvironmentProviders;
```

From @ngrx/effects:
```typescript
export declare function provideEffects(effects: Type<any>[]): EnvironmentProviders;
```

<!-- Phase 2 store symbols available for barrel export -->
// From actions/task.actions.ts: 15 action creators
// From reducers/task.reducer.ts: tasksFeature, taskAdapter, tasksReducer, tasksFeatureKey, TaskState, initialState, selectTasksState, selectLoading, selectError
// From selectors/task.selectors.ts: selectAllTasks, selectTasksByColumn, selectCountByPriority, selectCompletionRate
// From effects/task.effects.ts: TaskEffects
// From services/task-mock.service.ts: TaskMockService
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire store providers into app.config.ts</name>
  <files>src/app/app.config.ts</files>
  <action>
    Read src/app/app.config.ts first to see the current Phase 1 content, then replace with the updated version.

    The updated app.config.ts must:
    - Keep all existing providers: provideBrowserGlobalErrorListeners(), provideRouter(routes)
    - Add import { isDevMode } from "@angular/core" (add to existing @angular/core import)
    - Add import { provideStore } from "@ngrx/store"
    - Add import { provideEffects } from "@ngrx/effects"
    - Add import { provideStoreDevtools } from "@ngrx/store-devtools"
    - Add import { tasksReducer, tasksFeatureKey } from "@app/core/store/reducers/task.reducer"
    - Add import { TaskEffects } from "@app/core/store/effects/task.effects"
    - Add three providers:
      1. provideStore({ [tasksFeatureKey]: tasksReducer })
      2. provideEffects([TaskEffects])
      3. provideStoreDevtools({ maxAge: 25, logOnly: !isDevMode(), autoPause: true })

    Final app.config.ts content (follow exactly):
    ```typescript
    import { ApplicationConfig, isDevMode, provideBrowserGlobalErrorListeners } from "@angular/core";
    import { provideRouter } from "@angular/router";
    import { provideStore } from "@ngrx/store";
    import { provideEffects } from "@ngrx/effects";
    import { provideStoreDevtools } from "@ngrx/store-devtools";
    import { routes } from "./app.routes";
    import { tasksReducer, tasksFeatureKey } from "@app/core/store/reducers/task.reducer";
    import { TaskEffects } from "@app/core/store/effects/task.effects";

    export const appConfig: ApplicationConfig = {
      providers: [
        provideBrowserGlobalErrorListeners(),
        provideRouter(routes),
        provideStore({ [tasksFeatureKey]: tasksReducer }),
        provideEffects([TaskEffects]),
        provideStoreDevtools({
          maxAge: 25,
          logOnly: !isDevMode(),
          autoPause: true,
        }),
      ],
    };
    ```

    Use double-quote imports (OXfmt style).
    Do NOT add provideAnimations or provideAnimationsAsync — Angular Material schematic did not add these in Phase 1, keep consistent.
  </action>
  <verify>
    <automated>cd /Users/peterichardson/Repos/Carno-Frontend-Engineer-Exercise && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>npx tsc --noEmit exits 0; app.config.ts contains provideStore, provideEffects, provideStoreDevtools</done>
</task>

<task type="auto">
  <name>Task 2: Update store barrel index with all Phase 2 public symbols</name>
  <files>src/app/core/store/index.ts</files>
  <action>
    Replace the Phase 1 placeholder comment in src/app/core/store/index.ts with full re-exports.

    The barrel must re-export all symbols that Phase 3 (components) and Phase 4 (widgets) will need to import. Export from these four store subdirectories:

    ```typescript
    // Actions
    export * from "./actions/task.actions";

    // Reducer — state shape, adapter, and feature selectors
    export * from "./reducers/task.reducer";

    // Derived selectors
    export * from "./selectors/task.selectors";

    // Effects (exported for provideEffects in app.config.ts and potential testing)
    export { TaskEffects } from "./effects/task.effects";
    ```

    Use `export *` for actions, reducer, and selectors (they are the full public API).
    Use named `export { TaskEffects }` for effects to avoid re-exporting @ngrx/effects internals if any bleed through.

    After writing, run `npm test` to confirm no circular dependency or re-export conflict is introduced. If tsc reports a duplicate export error (unlikely but possible if createFeature auto-generates a name collision), resolve by using named re-exports instead of `export *` for the affected file.
  </action>
  <verify>
    <automated>cd /Users/peterichardson/Repos/Carno-Frontend-Engineer-Exercise && npm test 2>&1 | tail -25</automated>
  </verify>
  <done>npm test exits 0; store/index.ts re-exports actions, reducer, selectors, and TaskEffects; Phase 3 can import selectTasksByColumn from '@app/core/store'</done>
</task>

</tasks>

<verification>
Full Phase 2 integration verification:
1. `npx tsc --noEmit` — exits 0
2. `npm test` — all tests green (18+ tests total across all spec files)
3. `ng build` — build completes without errors (verifies runtime DI graph is complete)
4. Confirm store wired: `grep "provideStore\|provideEffects\|provideStoreDevtools" src/app/app.config.ts`
5. Confirm barrel exports all layers: `grep "export" src/app/core/store/index.ts | head -10`
6. Spot-check selector import path: `node -e "require('./dist/...')"` — or trust tsc --noEmit + ng build
</verification>

<success_criteria>
- app.config.ts: provideStore, provideEffects([TaskEffects]), provideStoreDevtools all registered
- store/index.ts: re-exports actions, reducer, selectors, TaskEffects
- npm test exits 0 — all Phase 2 tests pass (mock service: 4, reducer: 7, selectors: 4, effects: 3 = 18+ new tests on top of Phase 1 baseline)
- ng build exits 0 — full compile-time proof of correct DI wiring
</success_criteria>

<output>
After completion, create `.planning/phases/02-ngrx-store/02-06-SUMMARY.md`
</output>
