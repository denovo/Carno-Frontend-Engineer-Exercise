---
phase: 02-ngrx-store
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/constants.ts
  - src/app/core/services/mock-data.ts
  - src/app/core/services/task-mock.service.ts
  - src/app/core/services/task-mock.service.spec.ts
autonomous: true
requirements: [NGR-14]

must_haves:
  truths:
    - "TaskMockService.loadTasks() returns an Observable<Task[]> after a configurable delay"
    - "TaskMockService.moveTask() returns Observable<void> and throws when shouldFail is true"
    - "Setting latencyMs = 0 on the service instance makes tests run synchronously (no wall-clock wait)"
    - "MOCK_BOARD seed data contains 3 columns (Todo, In Progress, Done) with DONE_COLUMN_ID matching the constant"
    - "DONE_COLUMN_ID constant is exported from core/constants.ts"
  artifacts:
    - path: src/app/core/constants.ts
      provides: DONE_COLUMN_ID constant alongside existing OVERDUE_THRESHOLD_DAYS
      contains: "DONE_COLUMN_ID"
    - path: src/app/core/services/mock-data.ts
      provides: MOCK_BOARD seed Board with Column[] and Task[] covering all 4 Priority values
    - path: src/app/core/services/task-mock.service.ts
      provides: "@Injectable TaskMockService with all 5 CRUD methods"
      exports: [TaskMockService]
    - path: src/app/core/services/task-mock.service.spec.ts
      provides: Vitest unit tests confirming delay and failure paths
  key_links:
    - from: src/app/core/services/task-mock.service.ts
      to: src/app/core/services/mock-data.ts
      via: "import { MOCK_BOARD }"
      pattern: "MOCK_BOARD"
    - from: src/app/core/services/task-mock.service.ts
      to: src/app/core/constants.ts
      via: "indirect — DONE_COLUMN_ID used by Phase 2 Plan 04 selector tests"
---

<objective>
Create the local mock service layer that all other Phase 2 plans depend on for testing: seed data, the TaskMockService with configurable latency and failure, and the DONE_COLUMN_ID constant.

Purpose: Everything in Phase 2 (reducer tests, selector tests, effect tests) either imports from the mock service or uses the DONE_COLUMN_ID constant. Getting these right first prevents rework in dependent plans.
Output: task-mock.service.ts, mock-data.ts, updated constants.ts, and a passing spec file.
</objective>

<execution_context>
@/Users/peterichardson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterichardson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ngrx-store/02-RESEARCH.md

<interfaces>
<!-- Types from Phase 1 — import via @app/shared/models -->

From src/app/shared/models/index.ts:
```typescript
export { Board } from "./board.model";
export { Column } from "./column.model";
export { Task } from "./task.model";
export { Priority } from "./priority.enum";
export { TaskAction } from "./task-action.model";
```

From src/app/shared/models/task.model.ts:
```typescript
export interface Task {
  id: string;
  title: string;
  description?: string;
  columnId: string;
  priority: Priority;
  assignee?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

From src/app/shared/models/board.model.ts:
```typescript
export interface Board {
  id: string;
  name: string;
  columns: Column[];
}
```

From src/app/shared/models/column.model.ts:
```typescript
export interface Column {
  id: string;
  name: string;
  order: number;
}
```

From src/app/shared/models/priority.enum.ts:
```typescript
export enum Priority {
  Low = "LOW",
  Medium = "MEDIUM",
  High = "HIGH",
  Critical = "CRITICAL",
}
```

From src/app/core/constants.ts (current):
```typescript
export const OVERDUE_THRESHOLD_DAYS = 7;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DONE_COLUMN_ID to constants and create mock seed data</name>
  <files>src/app/core/constants.ts, src/app/core/services/mock-data.ts</files>
  <behavior>
    - DONE_COLUMN_ID must equal 'col-done' (this exact value used by all selector tests)
    - MOCK_BOARD must have columns with ids: 'col-todo', 'col-in-progress', 'col-done'
    - Seed tasks must cover all 4 Priority values (Low, Medium, High, Critical)
    - At least 5 tasks total, distributed across columns — at least 1 task in 'col-done' for completion rate tests
    - Task createdAt/updatedAt are Date objects (not strings)
    - Board id = 'board-1', name = 'Petello Board'
  </behavior>
  <action>
    Append DONE_COLUMN_ID to src/app/core/constants.ts:
    ```typescript
    /** Column ID for the "Done" column — used by selectCompletionRate and mock seed data. */
    export const DONE_COLUMN_ID = "col-done";
    ```

    Create src/app/core/services/mock-data.ts with MOCK_BOARD constant:
    - Import Board, Column, Task, Priority from '@app/shared/models'
    - Import DONE_COLUMN_ID from '@app/core/constants'
    - Define 3 columns: { id: 'col-todo', name: 'Todo', order: 0 }, { id: 'col-in-progress', name: 'In Progress', order: 1 }, { id: DONE_COLUMN_ID, name: 'Done', order: 2 }
    - Define 6 tasks: 3 in col-todo (Low, Medium, High priority), 2 in col-in-progress (High, Critical), 1 in col-done (Medium)
    - All tasks have string ids like 'task-1' through 'task-6', real Date objects for createdAt/updatedAt (use `new Date('2026-01-01')` etc. for determinism)
    - Export: `export const MOCK_BOARD: Board & { tasks: Task[] }` — NOTE: this adds a `tasks` property for test convenience; Board interface only has columns, so define a local type or use intersection
    - Alternative cleaner approach: export MOCK_COLUMNS and MOCK_TASKS separately alongside MOCK_BOARD. This is cleaner for the mock service which needs both. Export all three named constants.
    - Use double-quote imports (OXfmt style)
  </action>
  <verify>
    <automated>cd /Users/peterichardson/Repos/Carno-Frontend-Engineer-Exercise && npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>npx tsc --noEmit exits 0; DONE_COLUMN_ID exported from constants.ts; MOCK_BOARD (or MOCK_COLUMNS + MOCK_TASKS) exported from mock-data.ts with 6 tasks across 3 columns</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TaskMockService and its spec</name>
  <files>src/app/core/services/task-mock.service.ts, src/app/core/services/task-mock.service.spec.ts</files>
  <behavior>
    - loadTasks(boardId): returns Observable<Task[]> — emits MOCK_TASKS after latencyMs delay; throws when shouldFail=true
    - moveTask(taskId, newColumnId): returns Observable<void> — emits void after delay; throws when shouldFail=true
    - addTask(task): returns Observable<Task> — creates Task with crypto.randomUUID(), new Date() for timestamps; throws when shouldFail=true
    - updateTask(update): returns Observable<void> — emits void after delay; throws when shouldFail=true
    - removeTask(taskId): returns Observable<void> — emits void after delay; throws when shouldFail=true
    - latencyMs defaults to 400 (production) — tests set to 0 for speed
    - shouldFail defaults to false
    - Service is @Injectable({ providedIn: 'root' })
    - Spec tests: (1) loadTasks with latencyMs=0 emits Task[], (2) moveTask with shouldFail=true errors, (3) addTask returns task with generated id
  </behavior>
  <action>
    Create src/app/core/services/task-mock.service.ts:
    - Import Injectable from '@angular/core'
    - Import Observable, of, throwError from 'rxjs'
    - Import delay from 'rxjs/operators'
    - Import Task from '@app/shared/models'
    - Import Update from '@ngrx/entity'
    - Import MOCK_TASKS (or MOCK_BOARD.tasks) from './mock-data'
    - latencyMs = 400 (public, tests override to 0)
    - shouldFail = false (public)
    - All methods: if (this.shouldFail) return throwError(() => new Error(...)).pipe(delay(this.latencyMs))
    - loadTasks: returns of([...MOCK_TASKS]).pipe(delay(this.latencyMs))
    - moveTask / updateTask / removeTask: returns of(undefined as void).pipe(delay(this.latencyMs))
    - addTask: generates new Task with crypto.randomUUID() for id, new Date() for timestamps; returns of(newTask).pipe(delay(this.latencyMs))

    Create src/app/core/services/task-mock.service.spec.ts:
    - Import describe, it, expect, beforeEach from 'vitest'
    - Import firstValueFrom from 'rxjs'
    - Instantiate service directly (new TaskMockService()) — no TestBed needed for pure service
    - Set service.latencyMs = 0 in beforeEach for fast tests
    - Test 1: loadTasks returns array with expected length
    - Test 2: loadTasks with shouldFail=true rejects with Error
    - Test 3: moveTask with shouldFail=false resolves to undefined (void)
    - Test 4: addTask returns task with auto-generated id (string) and matching title
  </action>
  <verify>
    <automated>cd /Users/peterichardson/Repos/Carno-Frontend-Engineer-Exercise && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>npm test exits 0; task-mock.service.spec.ts shows 4 tests passing; no TypeScript errors</done>
</task>

</tasks>

<verification>
Run full verification after both tasks:
1. `npx tsc --noEmit` — 0 errors
2. `npm test` — all tests green (existing 4 + new 4)
3. Confirm DONE_COLUMN_ID = 'col-done' in constants.ts
4. Confirm MOCK_TASKS has tasks in 'col-done' (needed for selectCompletionRate tests in Plan 04)
</verification>

<success_criteria>
- DONE_COLUMN_ID exported from core/constants.ts with value 'col-done'
- MOCK_BOARD/MOCK_COLUMNS/MOCK_TASKS exported from core/services/mock-data.ts; seed covers all 4 priorities
- TaskMockService: 5 methods, latencyMs public, shouldFail public, @Injectable providedIn root
- task-mock.service.spec.ts: 4 tests pass with latencyMs=0
- npm test green; tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-ngrx-store/02-01-SUMMARY.md`
</output>
